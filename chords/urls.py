from django.conf.urls import url
from . import views

app_name = 'chord_recognition'
urlpatterns = [
    #  url(r'^$', views.index, name='index'),
    #  url(r'^practise/$', views.PractiseView.as_view(), name='practise'),
]
